Bot is supposed to be a client or a script that automates things.

Things to add to make a bot
In Channel
	flag for if a bot is existing in the channel,

In Server
	if bot flag from channel is on everything that a client types to the channel should go through bot or could be just line[0] for bot commands everything if its a filter bot.

bot feats
	!roll = returns a number randomly from 0-100
	!flip = flips a coin returns head or tails
	!help = help commands for bots
	automatically kicks if a client swears